<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <title>Notlarım</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(to right, #e6fff5, #ffe4e1);
      padding: 2rem;
    }
    .note-card {
      border-radius: 15px;
      background: #fff;
      padding: 1.5rem;
      margin-bottom: 1rem;
      box-shadow: 0 0 10px rgba(0,0,0,0.05);
    }
  </style>
</head>
<body>
  <div class="container">
    <h2 class="mb-4 text-center">Notlarım</h2>
    <% if (notes.length === 0) { %>
      <p class="text-center">Henüz notunuz yok.</p>
    <% } %>
    <% notes.forEach(note => { %>
      <div class="note-card">
        <h5><%= note.title %></h5>
        <p><%= note.content %></p>
      </div>
    <% }) %>
    <div class="text-center mt-4">
      <a href="/dashboard" class="btn btn-outline-primary">Geri Dön</a>
    </div>
  </div>
</body>
</html>
